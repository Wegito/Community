cd ~/community
cat > .env << 'ENV'
# Flask / App
FLASK_ENV=production
SECRET_KEY=VDSt1898  # gleich unten erzeugen wir einen sicheren Key
INVITE_CODE=meinhaus2025
ADMIN_EMAIL=dein.admin@mail.example

# Datenbank (SQLite im Projektverzeichnis)
SQLALCHEMY_DATABASE_URI=sqlite:///community.db

# (optional) Sicherheit / Cookies hinter Reverse Proxy
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_SAMESITE=Lax
PREFERRED_URL_SCHEME=https

# (optional) Zeitzone für Logs/Tasks
TZ=Europe/Berlin
ENV
